workflows:
  ios_dev_build:
    name: iOS Development Build
    max_build_duration: 120

    environment:
      flutter: stable

      ios_signing:
        distribution_type: development
        automatic_signing: true
        bundle_identifier: "com.thetork.kireibd"

      vars:
        BASEURL: ""
        BASEURLWEB: ""
        ANDROIDAPPLINK: ""
        IOSAPPLINK: ""

    scripts:
      - name: Flutter pub get
        script: |
          flutter pub get

      # Optional: generate .env for flutter_dotenv
      - name: Generate .env
        script: |
          echo "BASEURL=$BASEURL" > .env
          echo "BASEURLWEB=$BASEURLWEB" >> .env
          echo "ANDROIDAPPLINK=$ANDROIDAPPLINK" >> .env
          echo "IOSAPPLINK=$IOSAPPLINK" >> .env

      - name: Install CocoaPods
        script: |
          cd ios
          pod install
          cd ..

      - name: Build IPA (Development)
        script: |
          flutter build ipa --debug \
            --dart-define=BASEURL=$BASEURL \
            --dart-define=BASEURLWEB=$BASEURLWEB \
            --dart-define=ANDROIDAPPLINK=$ANDROIDAPPLINK \
            --dart-define=IOSAPPLINK=$IOSAPPLINK

    artifacts:
      - build/ios/ipa/*.ipa
